{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the link"
    },
    "type": {
      "type": "string",
      "description": "The type of the link (e.g., llm, sql, user_input)"
    },
    "description": {
      "type": "string",
      "description": "A description of what the link does"
    },
    "output_format": {
      "type": "string",
      "description": "The format of the output (e.g., json, text)"
    },
    "output_schema": {
      "type": "object",
      "description": "A JSON schema describing the output data"
    },
    "parameters": {
      "type": "object",
      "description": "A dictionary of parameters for the link"
    }
  },
  "required": [
    "name",
    "type"
  ]
}